# Research source backup — 14 September 2026

This commit adds 87 missing source and configuration files, including 71 Python files. Existing repository files are unchanged. Every copied file preserves its original contents.

## Blindspot code

The full blindspot implementation is in `day13/`. The entry point is `day13/run_all.py`. This includes cross-fitting, weights, specialist training, validation selection, evaluation, reporting, recovery, status and verification, plus both unit-test modules. Model definitions and earlier training dependencies are already present elsewhere in the repository.

## Day 12 code

Added source covers Task 1 distance ablation, Task 2 reference statistics, Task 3 preflight, original Task 4, Task 5 fusion/CSIQ evaluation, the distance-ablation implementation and the amended experiment. The amended pipeline entry point is `day12_distance_ablation_amended/run_pipeline.py`. Preserved copies of earlier source are included at their original relative paths.

## Backup scope

This is a source-code backup. It does not add research reports, score tables, dataset/pair manifests, audit outputs, frozen result files, checkpoints, binary arrays, images or ZIP archives. Previously tracked files remain as they were. Runtime inputs, trained models and experiment outputs must be restored separately from the research machine before reproducing an experiment. Some existing source/configuration files retain their original absolute paths; the backup does not alter them or bypass integrity checks.

The local training process was left running and the original workspace was not modified. This commit does not claim that the ongoing amended experiment is complete or that its model quality improved.

`BACKUP_2026-09-14_SHA256SUMS.txt` records hashes of all 87 copied source/configuration files. Verify from the repository root using `sha256sum -c BACKUP_2026-09-14_SHA256SUMS.txt`.

Before upload, every copied file matched its original SHA-256; all 71 added Python files passed syntax parsing; the selected source/configuration files passed a credential-pattern scan. Training code was not executed for this backup.
